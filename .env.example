# SeanLab Studio reads provider credentials from your existing shell environment.
# Do not put real credentials in this file or commit a populated .env.local.

# Optional legacy translation provider used only by projects that explicitly select it.
MIMO_API_KEY=
